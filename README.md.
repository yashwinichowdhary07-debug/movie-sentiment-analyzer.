# 🎬 Movie Sentiment Analyzer

A full-stack AI project that predicts the sentiment of a movie review (positive or negative).

## 🚀 Features
- End-to-end ML pipeline (train → save → predict)
- Flask REST API + Web UI
- Docker containerization
- GitHub Actions CI/CD
- 100% working locally

## ⚙️ Setup
```bash
python -m venv venv
source venv/bin/activate  # or .\\venv\\Scripts\\activate on Windows
pip install -r requirements.txt
python scripts/train.py --data data/small_sample.csv --output models/sentiment_model.joblib
flask run
